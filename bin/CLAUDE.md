# Runner

`php-cs-fixer-docker` runs php-cs-fixer inside the pinned image. Standalone; no Claude dependency.

Implements:
- cavekit-runner.md R1–R9 (containerized execution, mount fidelity, ownership, config resolution, custom fixers, operations, default scope, preflight errors, agent-friendly output)

Build tasks: T-002, T-004–T-009 (build-site.md)
